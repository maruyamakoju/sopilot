# =============================================================================
# .dockerignore -- keep the build context small and exclude secrets / large
# files that must never be baked into the production image.
# =============================================================================

# ---- Version control -------------------------------------------------------
.git/
.gitignore
.gitattributes

# ---- Python bytecode / build artifacts ------------------------------------
__pycache__/
**/__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
build/
dist/
.eggs/
*.whl

# ---- Virtual environments --------------------------------------------------
.venv/
venv/
env/

# ---- Test suite and coverage -----------------------------------------------
tests/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
coverage.xml

# ---- Documentation ---------------------------------------------------------
docs/

# ---- Research / utility scripts (not needed at runtime) --------------------
scripts/

# ---- Data directories (mounted as volumes, never baked in) -----------------
data/
data*/
artifacts/

# ---- SQLite databases ------------------------------------------------------
*.db
*.db-shm
*.db-wal

# ---- Video assets (large files) --------------------------------------------
poc_videos/
poc_videos_cleaning/
poc_videos_tesda/
*.mp4
*.webm
*.avi
*.mov

# ---- Environment / secrets -------------------------------------------------
.env
.env.*
*.pem
*.key
*.crt

# ---- IDE / editor files ----------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~

# ---- Docker files (avoid recursive context) --------------------------------
Dockerfile
docker-compose.yml
docker-compose.*.yml
docker-entrypoint.sh
.dockerignore

# ---- CI / monitoring config ------------------------------------------------
.github/
monitoring/

# ---- OS artefacts ----------------------------------------------------------
Thumbs.db
.DS_Store

# ---- Temporary build bundles -----------------------------------------------
_tmp_*/
