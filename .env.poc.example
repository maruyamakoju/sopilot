# Insurance MVP PoC — Environment Configuration
# Copy to .env.poc: cp .env.poc.example .env.poc
# Then run: docker compose -f docker-compose.poc.yml --env-file .env.poc up

# ---------------------------------------------------------------------------
# Dashboard
# ---------------------------------------------------------------------------
# Port for Streamlit dashboard (default: 8501)
DASHBOARD_PORT=8501

# ---------------------------------------------------------------------------
# VLM Backend (mock | real)
# ---------------------------------------------------------------------------
# mock  = No GPU required. Uses deterministic rule-based assessment.
# real  = Requires NVIDIA GPU with >=14GB VRAM. Uses Qwen2.5-VL-7B-Instruct.
# Note: "replay" mode is always available in the dashboard UI (loads JSON files).
INSURANCE_VLM_BACKEND=mock

# ---------------------------------------------------------------------------
# GPU (only needed when INSURANCE_VLM_BACKEND=real)
# ---------------------------------------------------------------------------
# Hugging Face cache dir (model weights ~15GB, downloaded on first run)
# HF_HOME=/path/to/huggingface/cache
