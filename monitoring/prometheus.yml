global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: sopilot
    static_configs:
      - targets:
          - sopilot:8000   # service name from docker-compose
    metrics_path: /metrics
    scrape_interval: 15s
